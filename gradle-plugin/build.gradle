apply plugin: 'java-gradle-plugin'
apply plugin: 'groovy'

version = '0.1-SNAPSHOT'
description = 'TODO'

ext {
    projectName = "${rootProject.name}Gradle"
    artifactId = 'gradle-plugin'
}

dependencies {
    compile localGroovy()
}
